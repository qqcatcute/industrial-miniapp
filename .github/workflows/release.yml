name: "Publish Release"

on:
  push:
    tags:
      - "v*"

jobs:
  publish-tauri:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: "macos-latest"
            # 修改点 1：去掉复杂的 universal 目标，先打出基础包
            args: ""
          - platform: "windows-latest"
            args: ""

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          # 修改点 2：显式添加 Mac 所需的两个目标，确保万无一失
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: install frontend dependencies
        working-directory: ./im-collab-ai-assistant-client 
        run: npm install

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: ./im-collab-ai-assistant-client
          tagName: v__VERSION__
          releaseName: "Agent App v__VERSION__"
          releaseBody: "自动生成的跨平台版本。"
          args: ${{ matrix.args }}
